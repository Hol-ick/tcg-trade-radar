# shadcn/ui 도입 스킬

## 사용 시점

다음 작업에 사용한다.

- TCG Trade Radar의 탐색기, 수집·로그, 보유 카드 수요, 거래 동향 화면을 새로 만들거나 개편할 때
- 버튼, 탭, 표, 필터, 다이얼로그, 명확한 빈 상태·오류 상태가 필요한 UI를 추가할 때
- 디자인 참고가 아니라 실제 React 기반 웹 UI 컴포넌트를 프로젝트에 도입할 때

## 실행 절차

1. 현재 대상이 tkinter인지 React/Next.js/Vite인지 확인한다. tkinter 대상이면 shadcn 코드를 직접 설치하지 않고 시각·상호작용 패턴만 네이티브 UI로 번역한다.
2. 공식 [컴포넌트 목록](https://ui.shadcn.com/)과 [설치 문서](https://ui.shadcn.com/docs/installation)에서 필요한 컴포넌트와 프레임워크 경로를 확인한다.
3. React 기반 기존 프로젝트라면 저장소의 패키지 매니저와 `components.json`, alias, CSS 토큰을 확인한 뒤 공식 CLI의 `shadcn@latest` 흐름 또는 공식 수동 설치 절차를 사용한다.
4. 생성·복사된 파일을 읽고 프로젝트의 데이터 계약, 상태 관리, 접근성, 로딩·오류·빈 상태에 맞게 최소 수정한다.
5. 원본 예제의 임시 데이터, 외부 링크, 비밀값, 서비스 종속 코드를 제거하고 TCG Trade Radar의 실제 API와 연결한다.
6. 컴포넌트 단위 확인, 전체 테스트, 가능하면 실제 화면 실행과 키보드·좁은 화면 확인을 수행한다.
7. 사용한 공식 컴포넌트 URL, 변경 파일, 검증 결과, 남은 호환성 위험을 AI_HUB worklog에 기록한다.

## 금지 사항

- 현재 Python/tkinter 앱에 React JSX, Tailwind 클래스, npm 의존성을 무단으로 섞지 않는다.
- shadcn 예제의 금융·분석용 데모 데이터나 문구를 제품 데이터처럼 남기지 않는다.
- 컴포넌트 도입을 이유로 기존 수집기, SQLite 스키마, 공개 데이터·개인정보 경계를 임의로 변경하지 않는다.
- UI 컴포넌트 설치와 실제 제품 UI 완성을 같은 것으로 보고하지 않는다. 설치, 연결, 실행, 검증을 각각 기록한다.
