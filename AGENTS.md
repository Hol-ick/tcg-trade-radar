# TCG Trade Radar 작업 지침

## UI 소스 및 컴포넌트 기준

- 새 UI를 만들거나 기존 탐색기 화면을 크게 개선할 때는 [shadcn/ui](https://ui.shadcn.com/)를 우선적인 컴포넌트·레이아웃 참고 및 도입 소스로 사용한다.
- 공식 컴포넌트 문서와 설치 안내를 먼저 확인한다: <https://ui.shadcn.com/docs/installation>.
- shadcn/ui 코드는 프로젝트가 소유하고 수정하는 방식으로 가져온다. 원격 페이지를 런타임에 iframe으로 삽입하거나 CDN 마크업에 의존하지 않는다.
- 도입한 컴포넌트는 프로젝트의 접근성, 키보드 조작, 데이터 상태, 오류·빈 상태와 함께 검증한다. 예제 화면의 문구·데이터·브랜딩을 그대로 복사하지 않는다.

## 현재 저장소의 기술 경계

- 현재 앱은 Python/SQLite/tkinter 데스크톱 앱이다. React/Tailwind용 shadcn 컴포넌트 코드를 tkinter 파일에 직접 붙여 넣지 않는다.
- 현재 tkinter UI에서는 shadcn의 정보 구조, 상태 표현, 간격·색상 계층을 참고해 네이티브 UI로 번역한다.
- React/Next.js/Vite 기반 웹 UI를 이 저장소에 추가하는 작업이 별도로 승인된 경우에만 공식 CLI 또는 해당 프레임워크의 기존 프로젝트 설치 절차를 따른다. 기존 Python 수집기·SQLite·API 계약은 유지한다.

## 변경·검증 규칙

- 컴포넌트를 추가하기 전에 대상 프레임워크, 패키지 매니저, 기존 스타일 토큰과 충돌 여부를 확인한다.
- 추가한 컴포넌트와 커스터마이즈 범위를 변경 파일과 작업 기록에 남긴다.
- UI 변경 후에는 기존 수집기 테스트와 UI가 사용하는 API·상태 변환을 함께 검증한다.
- 공개 게시글 수집 범위, 개인정보 최소 수집, 로그인·CAPTCHA 우회 금지 경계를 UI 변경으로 약화하지 않는다.
